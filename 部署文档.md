# OPPO Reno12（ColorOS 15.0）离线 APK 大包与部署文档

本文档用于把当前网页项目打包为 **仅本机使用** 的安卓 APK，满足以下目标：

- 只安装在你自己的 OPPO Reno12 上
- 不发布、不上架（不走应用商店）
- 不连服务器、不开公网
- 可离线使用
- 数据只保存在手机本地

---

## 1. 方案说明（本项目推荐）

采用 **Android Studio + WebView + 本地 assets** 方案：

- 将 `index.html`、`app.js` 等网页文件打进 APK 的 `assets/` 目录
- App 启动后通过 `file:///android_asset/index.html` 打开页面
- 页面数据通过 IndexedDB 存在手机本地 WebView 沙箱

这意味着：

- 不需要服务器
- 即使手机断网也可正常使用
- 卸载 App 会清空本地数据

---

## 2. 离线大包内容清单

建议最终交付一个目录（或压缩包）包含以下内容：

```text
offline-release/
  android-project/               # Android Studio 工程
  release-apk/
    BookingApp-release-v1.0.0.apk
  web-assets-source/             # 当前网页源码备份
    index.html
    styles.css
    app.js
    db.js
    sw.js
    manifest.json
    icons/
      icon-192.png
      icon-512.png
      apple-touch-icon.png
  docs/
    部署文档.md
```

最少可交付物：

- 已签名 `release apk`
- 对应 keystore 的安全备份（非常重要，后续升级要用同一签名）
- 本文档

---

## 3. 环境准备

1. 电脑安装 Android Studio（稳定版即可）。
2. 电脑安装 Android SDK Platform + Build Tools（Android Studio 首次构建会提示安装）。
3. OPPO Reno12（ColorOS 15.0）开启开发者选项与 USB 调试（建议）。

### 3.1 OPPO Reno12 开启 USB 调试

1. 手机进入：`设置 -> 关于本机 -> 版本信息`。
2. 连续点击“版本号”开启开发者模式。
3. 返回：`设置 -> 其他设置 -> 开发者选项`。
4. 打开：`USB 调试`。

提示：不同批次系统菜单名称可能有轻微差异，但关键项是“开发者选项”和“USB 调试”。

---

## 4. 新建 Android 工程

1. 打开 Android Studio -> `New Project` -> 选择 `Empty Views Activity`（或 `Empty Activity`）。
2. 参数建议：

- Name: `BookingApp`
- Package name: `com.gxr.bookingapp`（可自定义）
- Language: `Kotlin`
- Minimum SDK: `21` 或更高（建议 `24+`）

3. 完成创建并等待 Gradle 同步。

---

## 5. 拷贝网页文件到 APK（核心步骤）

在 Android 工程内创建目录：

`app/src/main/assets/`

把当前项目网页文件拷贝进去：

- `index.html`
- `styles.css`
- `app.js`
- `db.js`
- `sw.js`
- `manifest.json`
- `icons/` 整个目录

最终示例结构：

```text
app/src/main/assets/
  index.html
  styles.css
  app.js
  db.js
  sw.js
  manifest.json
  icons/icon-192.png
  icons/icon-512.png
```

---

## 6. 页面容器：WebView 配置

### 6.1 布局文件

文件：`app/src/main/res/layout/activity_main.xml`

```xml
<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</FrameLayout>
```

### 6.2 MainActivity（离线本地加载）

文件：`app/src/main/java/你的包名/MainActivity.kt`

```kotlin
import android.annotation.SuppressLint
import android.os.Bundle
import android.webkit.WebChromeClient
import android.webkit.WebResourceRequest
import android.webkit.WebSettings
import android.webkit.WebView
import android.webkit.WebViewClient
import androidx.appcompat.app.AppCompatActivity

class MainActivity : AppCompatActivity() {

    @SuppressLint("SetJavaScriptEnabled")
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        val webView = findViewById<WebView>(R.id.webView)

        webView.settings.apply {
            javaScriptEnabled = true
            domStorageEnabled = true
            databaseEnabled = true
            allowFileAccess = true
            allowContentAccess = false
            cacheMode = WebSettings.LOAD_DEFAULT
        }

        webView.webChromeClient = WebChromeClient()
        webView.webViewClient = object : WebViewClient() {
            override fun shouldOverrideUrlLoading(
                view: WebView?,
                request: WebResourceRequest?
            ): Boolean {
                // 仅允许加载本地 assets，禁止跳外网
                val url = request?.url?.toString() ?: return true
                return !url.startsWith("file:///android_asset/")
            }
        }

        webView.loadUrl("file:///android_asset/index.html")
    }
}
```

说明：

- 这里直接加载 `file:///android_asset/index.html`，不依赖服务器。
- 你的 `app.js` 中 service worker 注册失败会被 `catch` 吞掉，不影响本离线方案。

---

## 7. AndroidManifest 配置（只本地离线）

文件：`app/src/main/AndroidManifest.xml`

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="包间预定"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookingApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
```

注意：

- 不需要声明 `INTERNET` 权限（保持纯离线）。
- `android:allowBackup="false"` 可避免系统云备份本地数据（按需可改）。

---

## 8. App 图标（OPPO 桌面显示）

Web `manifest.json` 图标主要用于 PWA。当前方案是 APK，桌面图标由 Android 资源决定。

设置步骤：

1. Android Studio 中右键 `app` -> `New` -> `Image Asset`。
2. 选择前景图标源为 `icons/icon-512.png`（或更高分辨率）。
3. 生成 `ic_launcher` 与 `ic_launcher_round`。
4. 构建 APK 后，OPPO 桌面会显示该图标。

---

## 9. 构建与签名（生成可安装大包）

### 9.1 调试包（先验证）

- Android Studio -> `Build` -> `Build APK(s)`
- 输出位置通常在：

`app/build/outputs/apk/debug/app-debug.apk`

### 9.2 正式签名包（长期使用）

1. `Build` -> `Generate Signed Bundle / APK`。
2. 选择 `APK`。
3. 创建或选择 keystore（务必妥善保存）。
4. Build Variant 选 `release`。
5. 得到：

`app/build/outputs/apk/release/app-release.apk`

建议重命名：

`BookingApp-release-v1.0.0.apk`

---

## 10. 部署到 OPPO Reno12（ColorOS 15.0）

你有两种安装方式。

### 10.1 USB + ADB（推荐）

1. 手机开启 USB 调试并连接电脑。
2. 电脑执行：

```bash
adb devices
adb install -r BookingApp-release-v1.0.0.apk
```

3. 成功后在桌面打开 App。

### 10.2 手动拷贝 APK 安装

1. 将 APK 发到手机（微信/QQ/数据线都可）。
2. 手机上点开 APK。
3. 如系统拦截，进入对应开关页，允许该来源安装。
4. 安装完成后即可打开。

---

## 11. 离线验收清单（必须做）

安装完成后按以下顺序验证：

1. 打开 App，确认首页能正常渲染。
2. 新增/修改/删除一条预订数据。
3. 关闭 App 再打开，确认数据仍在。
4. 打开飞行模式（或断网）后再次操作，确认仍可用。
5. 重启手机后再次打开，确认数据仍在。

判定标准：

- 全程不依赖网络
- 数据跨重启保留
- 卸载 App 后数据清空（符合“仅本地存储”）

---

## 12. 升级发布（不丢数据）

当你修改网页逻辑后，需要重新打 APK。为避免覆盖失败或丢失数据：

1. **包名保持一致**（例如 `com.gxr.bookingapp`）。
2. **签名 keystore 保持一致**。
3. 提高 `versionCode`，更新 `versionName`。
4. 使用覆盖安装：

```bash
adb install -r 新版本.apk
```

满足以上条件时，通常会保留原有本地数据。

---

## 13. 常见问题排查

### 13.1 安装时报“解析包错误”

- 检查 APK 是否传输损坏（重新导出/重新传输）。
- 检查 Minimum SDK 是否高于手机系统能力（通常不会，Reno12 足够新）。

### 13.2 打开白屏

- 确认 `assets` 目录中 `index.html` 是否存在。
- 确认 `webView.loadUrl("file:///android_asset/index.html")` 拼写正确。
- 用 Android Studio Logcat 查看报错（JS 路径、资源 404）。

### 13.3 数据未保存

- 确认 `domStorageEnabled = true`。
- 确认未开启“每次退出清理数据”类逻辑。
- 确认不是卸载后重装（卸载会清空本地数据）。

### 13.4 图标不对

- 这是 Android 启动图标，不看网页 `manifest.json`。
- 重新生成 `ic_launcher` 后重装 App。

---

## 14. 安全与备份建议（本机场景）

1. keystore 至少备份两份（U 盘 + 云盘加密）。
2. App 不加 `INTERNET` 权限，保持纯离线面最小化。
3. 若将来需要导出数据，建议在网页里增加“导出 JSON 文件”功能。

---

## 15. 本项目当前文件对应关系

你当前网页源码（`/Users/gxr/book_set`）里已包含：

- `index.html`
- `styles.css`
- `app.js`
- `db.js`
- `sw.js`
- `manifest.json`
- `icons/`

这些文件直接复制到 Android `assets` 即可开始离线打包。

---

## 16. Android Studio 完整操作步骤（逐点击版）

本节把前面章节串成一次完整实操，按顺序点击即可完成从 0 到安装。

### 16.1 安装并初始化 Android Studio

1. 打开 Android Studio 安装包并完成安装。
2. 首次启动时选择标准配置（Standard）。
3. 进入主界面后点击：`More Actions -> SDK Manager`。
4. 在 `SDK Platforms` 勾选一个稳定版本（建议 Android 14 或更高）。
5. 在 `SDK Tools` 勾选：

- Android SDK Build-Tools
- Android SDK Platform-Tools
- Android SDK Command-line Tools（latest）

6. 点击 `Apply` 下载并安装。

### 16.2 新建项目

1. 回到欢迎页点击 `New Project`。
2. 模板选 `Empty Views Activity`（没有就选 `Empty Activity`）。
3. 按下列参数填写：

- Name：`BookingApp`
- Package name：`com.gxr.bookingapp`（可改，但后续保持不变）
- Save location：任意本地目录
- Language：`Kotlin`
- Minimum SDK：`API 24`（推荐）

4. 点击 `Finish`，等待右下角 Gradle 同步完成（出现 `Build finished`）。

### 16.3 切换到项目文件视图并创建 assets

1. 左侧项目树默认可能是 `Android` 视图，保持这个视图即可。
2. 展开：`app -> src -> main`。
3. 在 `main` 上右键：`New -> Directory`，输入 `assets`。
4. 在 `assets` 上右键：`Reveal in Finder`（macOS）或 `Show in Explorer`（Windows）。
5. 将你当前项目中的文件复制到该目录：

- `index.html`
- `styles.css`
- `app.js`
- `db.js`
- `sw.js`
- `manifest.json`
- `icons` 文件夹

6. 回到 Android Studio，右键 `assets` -> `Synchronize`（或直接等待自动刷新）。

### 16.4 配置页面布局

1. 打开：`app/src/main/res/layout/activity_main.xml`。
2. 切到 `Code` 视图，清空原内容。
3. 粘贴本文档第 6.1 节 XML。
4. 保存文件（`Command + S` / `Ctrl + S`）。

### 16.5 配置 MainActivity

1. 打开：`app/src/main/java/<你的包名>/MainActivity.kt`。
2. 全量替换为本文档第 6.2 节 Kotlin 代码。
3. 点击右上角 `Sync Now`（如果出现提示）。
4. 确认无红色报错；若有，先看 `import` 是否自动补齐。

### 16.6 配置 AndroidManifest

1. 打开：`app/src/main/AndroidManifest.xml`。
2. 将 `<application>` 内容按本文档第 7 节调整。
3. 检查以下两点：

- 存在启动 Activity（`MAIN` + `LAUNCHER`）
- 没有 `INTERNET` 权限（保持纯离线）

4. 保存文件。

### 16.7 设置 App 图标（Android 启动图标）

1. 在左侧选中 `app`。
2. 菜单：`File -> New -> Image Asset`（或右键 `app -> New -> Image Asset`）。
3. `Asset Type` 选 `Image`。
4. `Path` 选择你的 `icons/icon-512.png`。
5. `Name` 使用默认：`ic_launcher`。
6. 点击 `Next -> Finish`。
7. 生成后确认 `mipmap-*` 目录出现 `ic_launcher` 与 `ic_launcher_round`。

### 16.8 先构建调试包验证

1. 菜单：`Build -> Build APK(s)`。
2. 构建完成后右下角提示点 `locate`，找到：

`app/build/outputs/apk/debug/app-debug.apk`

3. 把该 APK 安装到手机先做功能验证（见第 11 节）。

### 16.9 生成正式签名 APK（长期使用）

1. 菜单：`Build -> Generate Signed Bundle / APK`。
2. 选 `APK`，点 `Next`。
3. 首次打包点 `Create new...` 创建 keystore：

- Key store path：例如 `~/keystore/bookingapp.jks`
- Password：设置并记牢
- Key alias：例如 `bookingapp`
- Key password：设置并记牢
- Validity：建议 `25` 年以上
- 组织信息可按需填写

4. 点击 `OK` 后回到向导，确认 keystore 与 alias 已选中。
5. 点击 `Next`，选择：

- Build variant：`release`
- Signature Versions：`V1` + `V2` 都勾选

6. 点击 `Finish` 开始构建。
7. 输出目录通常为：

`app/build/outputs/apk/release/app-release.apk`

8. 重命名为：`app-debug.apk` 便于管理。

### 16.10 安装到 OPPO Reno12（ColorOS 15.0）

1. 手机开启 `USB 调试` 并连接电脑。
2. 终端执行：

```bash
adb devices
adb install -r BookingApp-release-v1.0.0.apk
```

3. 手机端如有安装拦截提示，按系统引导允许当前来源安装。
4. 安装完成后在桌面打开 App。

### 16.11 版本升级（以后每次发新包）

1. 修改网页代码并替换 `assets` 文件。
2. 在 `app/build.gradle(.kts)` 增加 `versionCode`，更新 `versionName`。
3. 继续使用同一个包名和同一个 keystore 重新生成 `release apk`。
4. 执行覆盖安装：

```bash
adb install -r 新版本.apk
```

5. 打开 App 验证旧数据是否保留。
