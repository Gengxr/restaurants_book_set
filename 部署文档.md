# 老街坊劈柴院订桌系统服务器部署文档

当前服务器已采用直部署方式运行：

- 线上地址：`https://www.gxr123.cn/`
- 超级管理员后台：`https://www.gxr123.cn/admin`
- API 健康检查：`https://www.gxr123.cn/api/health`
- 应用目录：`/opt/booking-app`
- Node.js：`/opt/node-v24.16.0-linux-x64`
- 进程管理：`systemd` 服务 `booking-app`
- 反向代理与 HTTPS：`Nginx + 手动上传的 DigiCert 证书`
- 数据库：`/opt/booking-app/data/booking.sqlite`
- 备份目录：`/opt/booking-app/backups`

说明：服务器访问 Docker Hub 超时，因此实际部署没有使用 Docker/Caddy。仓库里的 Docker 文件仍保留，方便以后网络条件允许时切回容器部署。

## 常用命令

查看应用状态：

```bash
systemctl status booking-app
```

查看应用日志：

```bash
journalctl -u booking-app -n 100 --no-pager
```

重启应用：

```bash
systemctl restart booking-app
```

查看 Nginx 状态：

```bash
systemctl status nginx
nginx -t
```

手动备份数据库：

```bash
cd /opt/booking-app
npm run backup
```

检查 HTTPS：

```bash
curl -I https://www.gxr123.cn/api/health
```

当前证书文件：

```text
/etc/nginx/ssl/www.gxr123.cn/www.gxr123.cn.pem
/etc/nginx/ssl/www.gxr123.cn/www.gxr123.cn.key
```

当前证书信息：

```text
域名：www.gxr123.cn、gxr123.cn
签发方：DigiCert Encryption Everywhere DV TLS CA - G2
有效期：2026-05-23 至 2026-08-20
```

## 账号密码

当前版本不再要求首次登录修改密码。老板或超级管理员创建账号时需要手动填写密码；超级管理员后台会显示已保存的账号密码，旧版本已存在且未保存明文的账号需要重新设置后才可查看。

## 更新发布

把新代码上传并解压到 `/opt/booking-app` 后执行：

```bash
cd /opt/booking-app
npm ci --omit=dev
systemctl restart booking-app
```

如果静态资源缓存没有刷新，可以修改 `sw.js` 里的 `CACHE_NAME` 版本号。

## 域名说明

当前 `www.gxr123.cn` 已解析到服务器，并已部署上传的 HTTPS 证书。

裸域 `gxr123.cn` 当前没有 A 记录。若希望直接访问 `https://gxr123.cn/`，需要在 DNS 里增加：

```text
主机记录：@
记录类型：A
记录值：8.140.53.37
```

DNS 生效后，当前证书已经覆盖 `gxr123.cn`，通常无需重新签发，只需确认 Nginx 配置保留：

```nginx
server_name www.gxr123.cn gxr123.cn;
```

## 云监控

已安装阿里云云监控 C++ 插件 `argusagent`，服务名为 `cloudmonitor`。

查看状态：

```bash
systemctl status cloudmonitor
ps aux | grep argusagent | grep -v grep
```

查看日志：

```bash
tail -80 /usr/local/cloudmonitor/local_data/logs/argusagent.log
```

阿里云控制台监控数据通常需要等待几分钟才会刷新。
